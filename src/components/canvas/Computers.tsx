// src/components/canvas/Computers.tsx
'use client';

import React from "react";

// Simple placeholder component since we removed 3D dependencies
const ComputersCanvas = () => {
  return null; // Return nothing since we're not using 3D anymore
};

export default ComputersCanvas;